# snake-v3-by-me
мой первый большой проект в котором я столкнулся с кучей проблем и смог победить их!
# 🐍 Двойная Змейка: Битва с Ботом

<div align="center">

![Gameplay Demo](https://ibb.co/gbTDy2Zv)
*(скриншот игры в терминале)*

[![C++](https://img.shields.io/badge/C++-17-blue.svg)](https://isocpp.org/)
[![Windows](https://img.shields.io/badge/Platform-Windows-0078D6.svg)](https://windows.com)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

**Уникальный поворот классической змейки: сражайся против ИИ-бота за еду!**

</div>

## 🎮 Особенности игры

### ⚔️ **Дуэльный режим**
- **Игрок (O)** vs **Бот (V)** - две змейки на одном поле
- **Конкурентная система очков**: когда одна змейка ест, у другой отнимается очко
- **Динамический баланс**: нельзя просто собирать еду - нужно мешать противнику

### 🧠 **Умный (но не слишком) противник**
- Бот преследует еду по кратчайшему пути
- Избегает прямых столкновений с игроком
- Растёт так же, как и вы

### 🚀 **Прогрессивная сложность**
- Скорость игры плавно увеличивается с каждым съеденным фруктом
- Максимальная длина ограничена 33 сегментами
- 5 различных условий проигрыша

## 📦 Установка и запуск

### Требования
- Windows 10/11
- Компилятор C++ с поддержкой C++17
- Консоль с поддержкой Unicode

### Сборка и запуск
```bash
# Клонирование репозитория
git clone https://github.com/ваш-username/double-snake.git
cd double-snake

# Компиляция (пример для MinGW)
g++ -o snake.exe main.cpp game.cpp snake.cpp buffer.cpp -std=c++17

# Запуск
snake.exe
```

Или используйте CMake:
```cmake
mkdir build && cd build
cmake ..
make
./snake
```

## 🕹️ Управление

| Клавиша | Действие |
|---------|----------|
| **W** | Движение вверх |
| **A** | Движение влево |
| **S** | Движение вниз |
| **D** | Движение вправо |
| **X** | Экстренный выход |
| *(Авто)* | Бот управляется автоматически |

**Размер поля**: 24×9 клеток (оптимально для консоли)

## 🏆 Система очков

```cpp
// Когда ИГРОК ест фрукт:
score++;      // игрок +1
vscore--;     // бот -1

// Когда БОТ ест фрукт:
vscore++;     // бот +1
score--;      // игрок -1
```

**Условия поражения:**
1. Столкновение со стеной
2. Столкновение с собственным хвостом
3. Столкновение с хвостом бота
4. Столкновение "голова к голове" с ботом
5. Достижение -10 очков (бот съел 10 фруктов больше вас)

**Рекорды** автоматически сохраняются в `score.txt`

## 🧩 Архитектура проекта

### Структура кода
```
double-snake/
├── main.cpp          # Точка входа, инициализация
├── game.cpp         # Игровой цикл, координация
├── snake.cpp        # Логика обеих змей
├── buffer.cpp       # Система отрисовки в консоли
├── snake.h          # Класс snakecl
├── game.h           # Класс game
├── buffferr.h       # Класс buffer
└── score.txt        # Файл с рекордами
```

### Ключевые классы

#### `snakecl` - сердце игры
```cpp
class snakecl {
    // Игрок
    int snakex, snakey;          // Позиция головы
    int xvostx[50], xvosty[50];  // Хвост игрока
    int xvostlen;                // Длина хвоста
    
    // Бот
    float vsnakex, vsnakey;      // Позиция бота (float для плавности)
    int vxvostx[50], vxvosty[50]; // Хвост бота
    int vxvostlen;               // Длина хвоста бота
    
    // Общее
    int fruitx, fruity;          // Позиция фрукта
    double controlfps;           // Скорость игры
};
```

#### `buffer` - система отрисовки
```cpp
class buffer {
    char buf[9][25];             // Виртуальный экран
    void drawonbuf(x, y, sym);   // Отрисовка символа
    void onscreen();             // Вывод на реальный экран
    void bufferr();              // Очистка буфера
};
```

### Игровой цикл
```cpp
while (!gameOver) {
    // 1. Очистка и подготовка буфера
    // 2. Проверка столкновений и условий проигрыша
    // 3. Отрисовка всех элементов
    // 4. Обработка ввода игрока
    // 5. Логика бота
    // 6. Проверка поедания фруктов
    // 7. Вывод информации (счет, рекорд)
    // 8. Обновление позиций с учетом FPS
}
```

## 🐛 Известные особенности

### 1. **Плавное движение бота**
Бот использует `float` для координат, что дает:
- ✅ Плавное движение к цели
- ❌ Сложности при проверке столкновений
- ❌ Несоответствие с целочисленной сеткой

### 2. **Порядок обновления хвоста** (ИСПРАВЛЕНО!)
```cpp
// Критически важная последовательность:
1. Запомнить старую позицию хвоста
2. Обновить позицию головы
3. Сдвинуть сегменты хвоста
4. Добавить новый сегмент на старую позицию
```
*(Исторический баг: неправильный порядок вызывал артефакты роста)*

### 3. **FPS-контроль**
```cpp
// Адаптивная скорость:
controlfps -= 0.0020f;  // Ускоряется с каждым фруктом
// Минимальная задержка: ~0.03 секунды
```

## 🚀 Планы развития

### Версия 2.0
- [ ] **Цветовая дифференциация** змей
- [ ] **Несколько типов фруктов** с разными эффектами
- [ ] **Препятствия** на поле
- [ ] **Улучшенный ИИ** с паттернами движения

### Версия 3.0
- [ ] **Сетевой режим** (PvP)
- [ ] **Режим выживания** с растущей сложностью
- [ ] **Кастомизация** (символы, цвета, размер поля)
- [ ] **Система достижений**

## 🤝 Вклад в проект

Приветствуются любые улучшения:

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в форк (`git push origin feature/amazing`)
5. Создайте Pull Request

**Особенно нужны:**
- Исправление проверки коллизий с ботом
- Перенос на кроссплатформенные библиотеки (ncurses)
- Балансировка игрового процесса

## 📚 Чему научился проект

### Технические навыки:
- **Управление состоянием** двух независимых сущностей
- **Адаптивный FPS-контроль** для плавности
- **Работа с файловой системой** для сохранения прогресса
- **Консольная графика** через буферизацию вывода

### Архитектурные уроки:
- Разделение логики и отрисовки
- Важность порядка операций в игровом цикле
- Балансировка сложности ИИ

## 🏗️ Технический стек

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Язык** | C++17 | Основная логика |
| **Графика** | WinAPI Console | Отрисовка в консоли |
| **Ввод** | conio.h (_kbhit) | Обработка клавиш |
| **Время** | clock() из ctime | FPS-контроль |
| **Хранение** | fstream | Сохранение рекордов |

## 📄 Лицензия

MIT License - смотрите файл [LICENSE](LICENSE) для подробностей.


---

<div align="center">

### 💬 Цитата разработчика

> *"Этот проект начался как обычная змейка, но превратился в дуэль умов — человека и машины.  
> Иногда баги учат нас большему, чем работающий код. Спасибо нейросетям за помощь с тем самым багом роста!"*

**Удачи в битве против бота! Помните: -10 очков = поражение!**

⭐ **Если проект вам понравился, поставьте звезду!**

</div>

## 🎯 Быстрый старт для игроков

```bash
# Самый быстрый способ начать игру:
1. Скачай snake.exe из Releases
2. Запусти в терминале Windows
3. Управляй клавишами WASD
4. Не дай боту набрать 10 очков больше тебя!
```

**Совет:** Начинайте агрессивно — мешайте боту подбираться к фруктам!
